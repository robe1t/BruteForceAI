# Flask and extensions
Flask>=3.0.0
Flask-SocketIO>=5.3.0
Flask-CORS>=4.0.0
python-socketio>=5.10.0
# eventlet removed - using threading mode for Python 3.13+ compatibility

# Database
# sqlite3 is built-in Python module

# Playwright for browser automation
playwright>=1.40.0

# HTTP requests
requests>=2.31.0

# YAML configuration
PyYAML>=6.0.0

# OCR for captcha solving (use 1.6.0 for Python 3.13+)
ddddocr>=1.6.0
Pillow>=10.0.0

# Async support
aiohttp>=3.9.0
# asyncio is built-in Python module

# Type hints and validation (use 2.9+ for Python 3.13)
pydantic>=2.9.0

# Development and testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Utilities
python-dotenv>=1.0.0

# LLM API support
anthropic>=0.18.0
openai>=1.0.0